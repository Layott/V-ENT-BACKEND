# Files that land on a Linux server must keep LF endings. A CRLF shebang makes
# the kernel look for an interpreter called "bash\r" and the script dies with
# "bad interpreter: no such file or directory".
*.sh            text eol=lf
*.conf          text eol=lf
*.service       text eol=lf
deploy/**       text eol=lf
